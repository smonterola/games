import { PieceType, PieceColor } from "../components/Chessboard/Chessboard";

export default class Rules {
    isValidMove(x0: number, y0: number, x: number, y: number, type: PieceType, color: PieceColor) {
        return true;
    }
}